# DO NOT commit real secrets. Copy to .env and adjust values.
MONGO_URI="mongodb+srv://<user>:<pass>@cluster0.mongodb.net/crm?retryWrites=true&w=majority"
JWT_SECRET="change-me-please"
JWT_EXPIRES_MIN=60
ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
# Temporary admin credentials for initial login (replace with real user model later)
ADMIN_EMAIL="admin@mycrm.local"
ADMIN_PASSWORD="admin12345"
# Email SMTP (optional for dev)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="noreply@mycrm.com"
EMAIL_PASS="password"
# Twilio / WhatsApp (optional stubs)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM="whatsapp:+1234567890"
